export default {
   PRODUCTS(state) {
      return state.products;
    },
    CART(state) {
      return state.cart;
    },
    IS_MOBILE(state) {
      return state.isMobile;
    },
    IS_DESCTOP(state) {
      return state.isDesktop;
    },
}